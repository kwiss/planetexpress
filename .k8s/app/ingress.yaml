---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app-planetexpress-__CI_ENVIRONMENT_SLUG__
  labels:
    app: planetexpress-app
    ref: app-__CI_ENVIRONMENT_SLUG__-app
  annotations:
    kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - app-__CI_ENVIRONMENT_SLUG__.planetexpress.dev
    secretName: app-__CI_ENVIRONMENT_SLUG__.planetexpress.dev
  rules:
  - host: app-__CI_ENVIRONMENT_SLUG__.planetexpress.dev
    http:
      paths:
      - path: /
        backend:
          serviceName: app-planetexpress-__CI_ENVIRONMENT_SLUG__
          servicePort: 80